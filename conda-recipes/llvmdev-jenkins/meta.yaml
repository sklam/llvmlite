package:
  name: llvmdev
  version: "3.5p1"

source:
  fn: 3.5p1.tar.gz
  url: https://github.com/sklam/llvm/archive/3.5p1.tar.gz

build:
  number: 1

requirements:
  build:
    - system           [linux and not armv6]
  run:
    - system           [linux and not armv6]

#test:
  #commands:
    #- clang --help     [linux and not armv6]

about:
  home: http://llvm.org/
  license: Open Source (http://llvm.org/releases/3.5.0/LICENSE.TXT)
  summary: Development headers and libraries for LLVM
